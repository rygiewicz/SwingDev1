import React from 'react';

import PhotoList from './PhotoList';

const PhotoIndex = () => {
    return (
        <div id="photo-index">
            <PhotoList/>
        </div>
    );
};

export default PhotoIndex;