import React from 'react';

import PhotoList from './photo-list';

const PhotoIndex = () => {
    return (
        <div id="photo-index">
            <PhotoList/>
        </div>
    );
};

export default PhotoIndex;